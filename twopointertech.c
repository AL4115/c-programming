// wap to find the targeted value by summation of pairs (two pointer technique)
# include<stdio.h>
int main (){
   /* int arr[8]={1,2,3,5,7,10,11,15};
    int current_sum;
    int left;
    int right;
    while(left)
    }
    return 0;
}*/

// wap to find the address of an element given in the array
int arr[6]={2,3,4,7,9,1};
int BA = arr[0];
int a;
printf("enter the element no. a");
scanf("%d",&a);
int *ptr;
ptr=&a;
printf("%d",ptr);
return 0;
}

